query currentUser {
    currentUser {
        id
        email
        adUnitNamePrefix
        accounts {
            id
            email
            isReadyForReports
            enableImpressionLevelAdRevenue
        }
    }
}
