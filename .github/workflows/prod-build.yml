name: Prod build
on:
  push:
    branches:
      - "DO-2271/Move_to_github_action2"
#  release:
#    types: [published]

concurrency:
  group: ${{ github.workflow }}

jobs:
  deploy:
    name: Prod Build
    runs-on: [macos-latest]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install node
        uses: actions/setup-node@v4
        with:
          node-version: 14.20 #16.14
          cache: npm

      - name: Install dependencies
        run: npm i

      #- name: Build
      #  run: npm run build

      #- name: Copy _headers
      #  run: cp _headers ./build/
